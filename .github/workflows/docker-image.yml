name: get-current-weather-ci-build-dockerfile
on:
  push:
    branches: [ "main" ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file ./scripts/Dockerfile --tag get-current-weather:$(date +%s)
